from file1 import add,sub
from file2 import multi

if __name__ == "__main__":
    numbers = [1,2,3]
    
print("addition:",add(numbers))
print("substraction:", sub(numbers)
print("multiplication:", multi(numbers))
